# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/mcsupport"

# Next Auth Configuration
AUTH_SECRET="your_generated_secret_here" # Generate with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"

# Email Configuration (Nodemailer)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your_email@example.com"
SMTP_PASS="your_email_password"
SMTP_SECURE="false" # true for 465, false for other ports
SMTP_FROM="MC Support <support@multicomputos.com>"

# Sentry Configuration (Monitoring)
# Get these from your Sentry project settings
NEXT_PUBLIC_SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"
SENTRY_ORG="your-org-name"
SENTRY_PROJECT="your-project-name"
SENTRY_AUTH_TOKEN="your_auth_token"

# Application Settings
LOG_LEVEL="info" # debug, info, warn, error
